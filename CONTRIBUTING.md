# 贡献指南

## 开发规则

1. 在 `main-platform/` 或 `local-platform/` 内按各自 README 初始化环境。
2. 保持服务职责、鉴权、钱包、任务幂等和媒体落盘边界，不以假数据绕过真实链路。
3. 跨程序 DTO 先更新对应接口登记，再修改共享契约。
4. 新增源码使用中文职责注释，关键事务、退款、重试和回滚分支说明约束。
5. 提交前执行受影响项目的类型检查、测试和构建。

## 提交边界

不得提交生产地址、访问令牌、私钥、真实 `.env`、用户数据、数据库备份、模型权重、训练数据、生成媒体或构建目录。
