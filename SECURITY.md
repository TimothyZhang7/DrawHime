# 安全策略

## 漏洞报告

请通过 GitHub Security Advisory 私下报告安全问题，并说明受影响版本、复现步骤和影响范围。不要在公开 Issue 中粘贴凭证、用户媒体或生产日志。

## 敏感边界

- 所有真实环境变量、生产主机、数据库、Redis、对象存储和 GPU Runtime 地址均保留在部署方私有配置中。
- Token、卡密、验证令牌和会话凭证只保存哈希或进入一次性响应。
- 钱包写入、退款、任务终态和跨平台回调保持事务化或幂等。
- GPU Runtime、数据库和对象存储管理端口不得直接暴露公网。
